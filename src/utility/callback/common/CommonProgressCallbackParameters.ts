/**
 * The set of callback parameters that are common to all progress callback
 * types.
 */

export type CommonProgressCallbackParameters = {
  /**
   * The current step associated with the progress state.
   */
  step: number;
};
